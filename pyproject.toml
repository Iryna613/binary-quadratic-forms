[project]
name = "topograph-analyzer"
version = "0.2.0"
description = "Interactive Conway topograph analyzer for binary quadratic forms"
requires-python = ">=3.10"
dependencies = [
    "streamlit>=1.36",
    "plotly>=5.20",
    "pandas>=2.0",
    "networkx>=3.2",
]
